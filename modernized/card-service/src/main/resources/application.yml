server:
  port: 8084

spring:
  application:
    name: card-service
  datasource:
    url: jdbc:postgresql://localhost:5432/carddemo
    username: carddemo
    password: carddemo
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: card
  flyway:
    schemas:
      - card
