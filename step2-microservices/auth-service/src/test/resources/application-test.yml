spring:
  datasource:
    url: jdbc:h2:mem:carddemo_auth_test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false
  security:
    user:
      name: test
      password: test

eureka:
  client:
    enabled: false
