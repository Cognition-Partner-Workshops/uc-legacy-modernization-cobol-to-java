spring.jackson.deserialization.fail-on-unknown-properties=true
server.port=8080
# carddemo.statements.dir=output/statements/
# The default above falls back to bundled mock statements when missing or empty.
