# Mock statements

These fixtures are for local viewer demonstrations only. The parent session should
delete this directory and configure `carddemo.statements.dir` to point at the real
generated statement output.
